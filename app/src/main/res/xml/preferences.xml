<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <CheckBoxPreference android:title="Enable geolocation tracking"
        android:defaultValue="true"
        android:summary="This option if selected will as far as geolocation of device changed move camera on map to new geo. Mainly it's every 5-7 seconds"
        android:key="geoUpdates" />


</PreferenceScreen>
